<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pallasli.oa">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".hr.AskForLeaveActivity"
            android:label="@string/title_activity_ask_for_leave"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".bpm.WorkbenchActivity"
            android:label="@string/title_activity_workbench"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".bpm.MyWorkbenchActivity"
            android:label="@string/title_activity_workbench"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".bpm.MyTodoActivity" />
        <activity android:name=".bpm.MyDoneActivity" />
        <activity android:name=".bpm.MyApplyActivity" />
        <activity
            android:name=".hr.HrActivity"
            android:label="@string/title_activity_hr"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>